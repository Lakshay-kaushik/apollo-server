type trainee {
    _id: ID!
    name: String!
    email: String!
    role: String
    originalId: ID
}
type User {
    id: ID!
    name: String!
    email: String!
    role: String
}

input LoginInput {
    email: String!
    password: String!
}

input UpdateUser {
    originalID: ID!
    name: String
    email: String
    password: String
}